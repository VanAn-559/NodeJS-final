<div class="welcome">
<div class="row mt-5 ">
  <div class="col-md-6 m-auto">
    <div class="card card-body card-welcome" style="margin-top: 75px;">
      <h1 class="text-center mb-3">
        <i class="fas fa-user-plus"></i>
      </h1>
       {{#if message}}
            <div class="alert {{messageClass}}" role="alert">
                {{message}}
            </div>
        {{/if}}
      <form action="/users/register" method="POST">
        <div class="form-group">
          <label for="name">Name</label>
          <input
            type="name"
            id="name"
            name="name"
            class="form-control"
            placeholder="Enter Name"
            value="{{users.name}}"
            {{!-- value="<%= typeof name != 'undefined' ? name : '' %>" --}}
          />
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-control"
            placeholder="Enter Email"
            {{!-- value="<%= typeof email != 'undefined' ? email : '' %>" --}}
            value="{{users.email}}"
          />
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="Create Password"
            {{!-- value="<%= typeof password != 'undefined' ? password : '' %>" --}}
            value="{{users.password}}"
          />
        </div>
        <div class="form-group">
          <label for="password2">Confirm Password</label>
          <input
            type="password"
            id="password2"
            name="password2"
            class="form-control"
            placeholder="Confirm Password"
            {{!-- value="<%= typeof password2 != 'undefined' ? password2 : '' %>" --}}
             value="{{users.password}}"
          />
          </div>
          <button type="submit" class="btn btn-block" style="background-color:  #e26c27;">
          Register
        </button>
      </form>
      <p class="lead mt-4">Have An Account? <a href="/users/login">Login</a></p>
    </div>
  </div>
</div>
</div>


  {{!-- <div class="row">
    <div class="col-md-6 m-auto">
      <div class="card card-welcome">
        <div class="card-body">
          <h1><i class="fab fa-node-js fa-3x"></i></h1>
          <p class="card-text">Create an account or login</p>
          <a href="/users/register" class="btn btn-block mb-2" style="background-color:  #e26c27;">Register</a>
          <a href="/users/login" class="btn btn-block" style="background-color:  #e26c27;">Login</a>
        </div>
      </div>
    </div>
  </div>
  </div> --}}